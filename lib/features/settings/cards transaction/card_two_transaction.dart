import 'package:flutter/material.dart';

class CardTwoTransaction extends StatelessWidget {
  const CardTwoTransaction({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 300,
      width: double.infinity,
      decoration: const BoxDecoration(
          color: Colors.red
      ),
    );;
  }
}
