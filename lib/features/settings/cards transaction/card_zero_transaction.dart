import 'package:flutter/material.dart';

class CardZeroTransaction extends StatelessWidget {
  const CardZeroTransaction({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 300,
      width: double.infinity,
      decoration: BoxDecoration(
          color: Colors.yellow
      ),
    );;
  }
}
